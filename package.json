{
  "name": "product-comparisons",
  "version": "1.0.0",
  "description": "Sync markdown files with Google Drive documents",
  "main": "dist/sync-docs.js",
  "scripts": {
    "build": "tsc",
    "sync": "npm run build && node dist/sync-docs.js sync",
    "pull": "npm run build && node dist/sync-docs.js pull",
    "sync:both": "npm run build && node dist/sync-docs.js both",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@tetrateio/scully-clients": "^0.2.5",
    "@types/glob": "^8.1.0",
    "@types/marked": "^5.0.2",
    "@types/node": "^24.3.2",
    "glob": "^11.0.3",
    "googleapis": "^159.0.0",
    "gray-matter": "^4.0.3",
    "marked": "^16.2.1",
    "typescript": "^5.9.2"
  }
}
